import numpy as np

np1 = np.array([-3, -2, -1, 0, 1, 2, 3, 4, 5])

# Absolute value
print(np.absolute(np1))
print(np1)

# Square Root
print(np.sqrt(np1))

# Min/Max values
print(np.max(np1))
print(np.min(np1))
